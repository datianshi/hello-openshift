apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: openshift-hello-build-prod-deploy-workspace
spec:
  resources:
    requests:
      storage: 5Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce